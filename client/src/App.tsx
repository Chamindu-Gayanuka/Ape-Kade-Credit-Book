import {BrowserRouter,Navigate,Route,Routes} from 'react-router-dom';import {AuthProvider,useAuth} from './contexts/AuthContext';import AppLayout from './layouts/AppLayout';import Login from './pages/Login';import Dashboard from './pages/Dashboard';import Customers from './pages/Customers';import CustomerDetail from './pages/CustomerDetail';import FinancialForm from './pages/FinancialForm';import Transactions from './pages/Transactions';import Reports from './pages/Reports';import Users from './pages/Users';import Audit from './pages/Audit';import Settings from './pages/Settings';import Profile from './pages/Profile';import {Spinner} from './components/UI';import {ThemeProvider} from './contexts/ThemeContext';
function Guard({admin=false,children}:{admin?:boolean;children:any}){const {user,loading}=useAuth();if(loading)return <Spinner/>;if(!user)return <Navigate to="/login" replace/>;if(admin&&user.role!=='ADMIN')return <Navigate to="/" replace/>;return children}function Router(){const {user,loading}=useAuth();return <Routes><Route path="/login" element={loading?<Spinner/>:user?<Navigate to="/"/>:<Login/>}/><Route element={<Guard><AppLayout/></Guard>}><Route index element={<Dashboard/>}/><Route path="customers" element={<Customers/>}/><Route path="customers/:id" element={<CustomerDetail/>}/><Route path="credit" element={<FinancialForm type="credit"/>}/><Route path="payment" element={<FinancialForm type="payment"/>}/><Route path="transactions" element={<Transactions/>}/><Route path="reports" element={<Reports/>}/><Route path="profile" element={<Profile/>}/><Route path="users" element={<Guard admin><Users/></Guard>}/><Route path="audit" element={<Guard admin><Audit/></Guard>}/><Route path="settings" element={<Guard admin><Settings/></Guard>}/></Route><Route path="*" element={<Navigate to="/"/>}/></Routes>}export default function App(){return <ThemeProvider><BrowserRouter><AuthProvider><Router/></AuthProvider></BrowserRouter></ThemeProvider>}
