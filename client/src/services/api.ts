import axios from 'axios';export const api=axios.create({baseURL:import.meta.env.VITE_API_URL||'/api',withCredentials:true,headers:{'Content-Type':'application/json'}});api.interceptors.response.use(r=>r,e=>{if(e.response?.status===401&&!location.pathname.includes('/login'))location.href='/login';return Promise.reject(e)});export const message=(e:any)=>e.response?.data?.message||'Something went wrong. Please try again.';
